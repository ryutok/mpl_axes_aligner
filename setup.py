from setuptools import setup

setup(name='mpl_axes_aligner',
      version='0.1',
      author='ryutok',
      # author_email='',
      url='https://github.com/ryutok/mpl_axes_aligner',
      description=('Adjust the plotting range of matplotlib.axes.Axes '
                   'objects to align the origins with the position'),
      # long_description=,
      # license=mit,
      py_modules=['axes_aligner'],
      )
